const CLIENT_JOIN_ROOM = 'client.JOIN_ROOM'
const CLIENT_CREATE_ROOM = 'client.CREATE_ROOM'
const CLIENT_PLAYER_READY = 'client.PLAYER_READY'

const DATA_ROOM_ID = 'data.ROOM_ID'
const DATA_MAP = 'data.MAP'
const DATA_PLAYER_COUNT = 'data.PLAYER_COUNT'
const DATA_PLAYER_ID = 'data.PLAYER_ID'
const DATA_GAME_STARTED = 'data.GAME_STARTED'

const MESSAGE_ROOM_FULL = 'message.ROOM_FULL'

module.exports = {
  CLIENT_CREATE_ROOM,
  CLIENT_JOIN_ROOM,
  CLIENT_PLAYER_READY,
  DATA_MAP,
  DATA_ROOM_ID,
  DATA_PLAYER_COUNT,
  DATA_PLAYER_ID,
  DATA_GAME_STARTED,
  MESSAGE_ROOM_FULL
}
